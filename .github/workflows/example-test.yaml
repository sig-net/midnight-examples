# Reusable per-example CI: one job that runs the SAME commands a developer
# types, parameterized by the example directory name (the `<example-dir>` in
# the root `compile:<example-dir>` / `test:<example-dir>` scripts —
# see AGENTS.md's root-script naming rule).
#
#   install → format:check → compact toolchain → compile (skip-zk) → lint →
#   zk keys (cached) → build → unit tests → compose up → e2e suite
#
# Caches (keyed on the pinned compiler version and the contract sources):
#   compact-*  : the compact toolchain (~/.compact + the launcher binary),
#                keyed on the pinned compiler version.
#   zk-keys-*  : the example contract's prover/verifier keys, keyed on the
#                compiler version + every .compact source in the repo; a hit
#                lets the suite's setup skip ~10 min of keygen via
#                TRUST_PREBUILT_ZK_KEYS (fresh by construction: the key
#                changes whenever any contract source changes).

name: example-test

on:
  workflow_call:
    inputs:
      example-dir:
        description: "The example's directory name under examples/ (e.g. erc20-vault)"
        required: true
        type: string
      full-suite:
        description: "Run the example's FULL e2e suite; false runs only the gate spec"
        required: false
        type: boolean
        default: false
      gate-spec:
        description: "Spec file (relative to the example's integration-tests package) run when full-suite is false"
        required: false
        type: string
        default: tests/happy-day-e2e.test.ts
    secrets:
      SEPOLIA_FORK_RPC_URL:
        description: "Sepolia RPC URL the local anvil forks from (see the fork-env comment below)"
        required: true

jobs:
  example:
    name: ${{ inputs.example-dir }} — format + lint + unit + e2e
    runs-on: ubuntu-latest
    # The claim/settle proofs are the pacing (and memory) item: a single one
    # peaks >12 GiB inside the proof server, so the default 4-core/16 GB
    # runner is marginal — happy-day-only as the PR gate keeps the wall clock
    # and OOM exposure down; the full suite is opt-in via `full-suite`.
    timeout-minutes: ${{ inputs.full-suite && 300 || 150 }}
    # Job-level so EVERY step (and every docker compose invocation) sees the fork URL. The
    # setup pipeline recreates the fakenet responder with `docker compose up`; if that step
    # lacks SEPOLIA_FORK_RPC_URL, compose re-renders the evm service WITHOUT --fork-url, drifts
    # from the running forked anvil, and recreates local-evm BARE — silently killing the fork.
    env:
      SEPOLIA_FORK_RPC_URL: ${{ secrets.SEPOLIA_FORK_RPC_URL }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: lts/*
          # corepack owns yarn here; the action's cache probe would run
          # yarn 1.x against the yarn-4 packageManager field and die
          package-manager-cache: false
      - name: Enable corepack (yarn 4 via the packageManager field)
        run: corepack enable
      - name: Restore the compact toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.compact
            ~/.local/bin/compact
          key: compact-${{ runner.os }}-0.33.0-rc.2
          restore-keys: compact-${{ runner.os }}-
      - name: Install the compact toolchain (pinned 0.33.0-rc.2, per AGENTS.md)
        run: |
          export PATH="$HOME/.local/bin:$HOME/.compact/bin:$PATH"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          echo "$HOME/.compact/bin" >> "$GITHUB_PATH"
          # Launcher pinned to compact-v0.5.1 (never releases/latest), the
          # same pin the protocol repo's workflows use.
          if ! command -v compact >/dev/null 2>&1; then
            curl --proto '=https' --tlsv1.2 -LsSf --retry 4 -o /tmp/compact-installer.sh \
              https://github.com/midnightntwrk/compact/releases/download/compact-v0.5.1/compact-installer.sh
            echo "85e74a53ae4a67b31fa4d854f3b6ffb4c29f7c53e3372c5008b6a4729a8b4a73  /tmp/compact-installer.sh" | sha256sum -c
            sh /tmp/compact-installer.sh
          fi
          # compactc 0.33.0-rc.2 is the PINNED compiler, part of the ledger-9
          # MATCHED SET (see docker-compose.yaml's image-tag comment: move it
          # together with the npm stack). It is a prerelease published on the
          # toolchain's LF home (LFDT-Minokawa/compact), which the launcher's
          # channel does not list: download it directly and let the launcher
          # adopt it. A bare `compact update` would install latest stable and
          # break the pin, so it is never run here.
          RC_DIR="$HOME/.compact/versions/0.33.0-rc.2/x86_64-unknown-linux-musl"
          if [ ! -x "$RC_DIR/compactc" ]; then
            mkdir -p "$RC_DIR"
            curl -fsSL --retry 4 -o /tmp/compactc-rc.zip \
              https://github.com/LFDT-Minokawa/compact/releases/download/compactc-v0.33.0-rc.2/compactc_v0.33.0-rc.2_x86_64-unknown-linux-musl.zip
            echo "3055ab92bbc8d5bb0d6282b661b83761d2a0de2ee37e21cf7107e25aaf2a9aad  /tmp/compactc-rc.zip" | sha256sum -c
            unzip -qo /tmp/compactc-rc.zip -d "$RC_DIR"
            chmod +x "$RC_DIR"/*
          fi
          # `compact update <ver>` adopts an already-on-disk version without
          # downloading; if the launcher refuses (version not in its
          # channel), wire the default symlinks it would have created.
          mkdir -p "$HOME/.compact/bin"
          compact update 0.33.0-rc.2 || ln -sfn "$RC_DIR"/* "$HOME/.compact/bin/"
          compact compile --version
      - name: Read the compact compiler version (part of the zk cache key)
        id: compact
        run: |
          # The zk-key cache must bust whenever the compiler BUILD changes,
          # but `compact compile --version` drops the -rc suffix (rc.0 and
          # rc.2 both print 0.33.0), so an rc bump would silently reuse the
          # old rc's cached managed/ output against a newer runtime. Read the
          # rc-qualified version from the default compiler's version directory
          # (…/versions/<version>/<arch>/compactc) instead.
          target="$(readlink -f "$(command -v compactc)")"
          version="$(basename "$(dirname "$(dirname "$target")")")"
          case "$version" in [0-9]*) ;; *) version="$(compact compile --version)" ;; esac
          echo "version=$(printf '%s' "$version" | tr -cd '[:alnum:]._-')" >> "$GITHUB_OUTPUT"
      - name: Install dependencies (locked by the committed yarn.lock)
        run: yarn install --immutable
      # Formatting needs nothing compiled, so it fails fast.
      - name: Check formatting (prettier)
        run: yarn format:check
      - name: Compile (skip-zk)
        run: yarn compile
      # Type-aware lint rules read the generated src/managed/ types the source
      # imports, so lint cannot run on a clean checkout before compile.
      - name: Lint (eslint, type-aware)
        run: yarn lint
      - name: Restore prebuilt zk proving keys
        id: zk
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: examples/${{ inputs.example-dir }}/contract/src/managed
          key: zk-keys-${{ runner.os }}-${{ inputs.example-dir }}-${{ steps.compact.outputs.version }}-${{ hashFiles('examples/*/contract/src/*.compact') }}
      # Inputs reach run blocks via env, never inline ${{ }} interpolation:
      # inline expansion into shell is a code-injection surface.
      - name: zk keygen (cache miss only)
        if: steps.zk.outputs.cache-hit != 'true'
        env:
          EXAMPLE_DIR: ${{ inputs.example-dir }}
        run: yarn "compile:${EXAMPLE_DIR}:zk"
      - name: Save zk proving keys (immediately — a later flow failure must not lose them)
        if: steps.zk.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: examples/${{ inputs.example-dir }}/contract/src/managed
          key: zk-keys-${{ runner.os }}-${{ inputs.example-dir }}-${{ steps.compact.outputs.version }}-${{ hashFiles('examples/*/contract/src/*.compact') }}
      - name: Build (typecheck, no emit)
        run: yarn build
      - name: Unit tests (simulator-only; e2e files self-skip without RUN_INTEGRATION_TESTS)
        run: yarn test
      - name: Start the local stack (midnight node, indexer, proof server, anvil forking Sepolia)
        # SEPOLIA_FORK_RPC_URL comes from the job-level env so this AND the setup's fakenet
        # recreate render the evm service identically-forked. REQUIRED — without it anvil runs
        # bare (no real tokens) and the fork suites fail. Set it as a repo Actions secret.
        run: |
          if [ -z "$SEPOLIA_FORK_RPC_URL" ]; then
            echo "::error::SEPOLIA_FORK_RPC_URL is empty — set it as a repo Actions secret (caller passes secrets: inherit)."
            exit 1
          fi
          # Fail fast if the fork args don't render, instead of an opaque bare-anvil failure later.
          docker compose config | grep -q -- '--fork-url' \
            || { echo "::error::compose rendered no --fork-url despite SEPOLIA_FORK_RPC_URL being set"; exit 1; }
          docker compose up -d
      - name: Wait for the stack
        run: |
          timeout 180 bash -c 'until curl -fsS http://127.0.0.1:9944/health >/dev/null 2>&1; do sleep 3; done'
          timeout 60 bash -c 'until curl -s -o /dev/null http://127.0.0.1:6300; do sleep 3; done'
          timeout 180 bash -c 'until curl -fs -X POST -H "content-type: application/json" -d "{\"query\":\"{__typename}\"}" http://127.0.0.1:8088/api/v3/graphql >/dev/null 2>&1; do sleep 3; done'
          # Forking fetches the pinned block before serving, so wait for anvil to answer.
          timeout 120 bash -c 'until curl -fsS -X POST -H "content-type: application/json" -d "{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"eth_chainId\",\"params\":[]}" http://127.0.0.1:8545 >/dev/null 2>&1; do sleep 3; done'
          curl -fsS -X POST -H 'content-type: application/json' \
            -d '{"jsonrpc":"2.0","id":1,"method":"eth_chainId","params":[]}' http://127.0.0.1:8545
      # The suite's own globalSetup does the rest: deploys the singleton + the
      # example contract, defaults ERC20_ADDRESS to real Sepolia USDC, creates
      # .env and appends the fakenet hand-off values, starts the fakenet
      # responder container, and deals the derived accounts ETH + USDC on the fork.
      - name: e2e suite — gate spec only
        if: inputs.full-suite != true
        env:
          TRUST_PREBUILT_ZK_KEYS: "1"
          EXAMPLE_DIR: ${{ inputs.example-dir }}
          GATE_SPEC: ${{ inputs.gate-spec }}
        # gate-spec is a space-separated list of spec files: split it into
        # one argument per file.
        run: |
          read -ra specs <<<"$GATE_SPEC"
          yarn "test:${EXAMPLE_DIR}:e2e" "${specs[@]}"
      - name: e2e suite — full
        if: inputs.full-suite == true
        env:
          TRUST_PREBUILT_ZK_KEYS: "1"
          EXAMPLE_DIR: ${{ inputs.example-dir }}
        run: yarn "test:${EXAMPLE_DIR}:e2e"
      - name: Collect service logs
        if: failure()
        # Per service, and TAILED. One unbounded `docker compose logs` across every service hit the
        # 120s bound every time and produced a 500 MB file that stopped 45 minutes before the
        # failure, because the proof server logs at debug. The end of each log is what diagnoses a
        # failure, so bound each one and keep its tail.
        run: |
          mkdir -p ci-logs
          for service in proof-server node indexer evm fakenet-responder; do
            timeout 30 docker compose --profile fakenet logs --no-color --tail 3000 "$service" \
              > "ci-logs/${service}.log" 2>&1 || true
          done
          timeout 60 docker ps -a > ci-logs/docker-ps.txt 2>&1 || true
          # Exit code, OOM kill and restart count per container: `docker ps` shows a container that
          # died and came back as merely "Up", which hides the death that failed the run.
          timeout 60 docker inspect $(docker ps -aq) \
            --format '{{.Name}} running={{.State.Running}} oomKilled={{.State.OOMKilled}} exitCode={{.State.ExitCode}} restarts={{.RestartCount}} startedAt={{.State.StartedAt}}' \
            > ci-logs/docker-state.txt 2>&1 || true
          timeout 30 free -m > ci-logs/memory.txt 2>&1 || true
      - name: Upload service logs
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ inputs.example-dir }}-e2e-logs
          path: ci-logs/
