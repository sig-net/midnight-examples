/**
 * Every path the SPA serves, as the single source of truth shared by the route
 * table and every {@link https://reactrouter.com | React Router} `Link`.
 * Anything not listed here falls through to the not-found view.
 */
export enum RoutePath {
  Home = "/",
}
